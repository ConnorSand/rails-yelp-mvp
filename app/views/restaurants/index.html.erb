<div class="container">
  <h1 >Restaurants in Canberra</h1>
  <%= link_to "Add a Restaurant", new_restaurant_path %>

  <% @restaurants.each do |restaurant| %>
    <h3><%= link_to restaurant.name, restaurant_path(restaurant.id) %></h3>
  <% end %>


</div>
